{% extends 'base.html.twig' %}

{% block title 'Create Tag' %}

    {% block body %}

        <div class="container">
            <h1>Create a tag</h1>
            {{ include ('admin/tag/_form.html.twig', {
                form: form,
                button: 'Create'
            }) }}
        </div>

    {% endblock %}