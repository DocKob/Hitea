{% extends 'base.html.twig' %}

{% block title device.name %}

    {% block body %}

        <div class="container">
            <div class="row">
                <div class="col-md-8">
                    <h1>{{ device.name }}</h1>
                </div>
                <div class="col-md-4"></div>
            </div>
        </div>

    {% endblock %}